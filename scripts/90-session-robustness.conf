# Installed to /etc/sddm.conf.d/90-session-robustness.conf by start_fresh.sh.
# Points SDDM at the wrapper next to this file rather than at its own
# /etc/sddm/wayland-session; see the comments in sddm-wayland-session.
[Wayland]
SessionCommand=/usr/local/bin/sddm-wayland-session
