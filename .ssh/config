# My generic SSH configuration, helpful for getting started.

Host *
     ControlMaster auto
     ControlPath ~/.ssh/%r@%h:%p
     ControlPersist 10m

Host self
     Hostname localhost
     User dmalyuta
