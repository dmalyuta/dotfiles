# journald's default SyncIntervalSec is 5 minutes. A resume that has to be
# ended with the power button therefore takes the whole record of itself with
# it, which is exactly what happened on 2026-09-05. Bound the loss to 10s.
[Journal]
SyncIntervalSec=10s
